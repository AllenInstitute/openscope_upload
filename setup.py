from setuptools import setup, find_packages

setup(
    name="openscope_upload",
    version="0.1.0",
    packages=find_packages(),
    description="Tools for OpenScope data upload and processing",
    author="Your Name",
    install_requires=[],
)